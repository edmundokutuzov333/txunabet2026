'use client';
import { useEffect,useState } from 'react';
import { Card,CardContent,CardHeader,CardTitle } from '@/components/ui/card';
import { Button } from '@/components/ui/button';
type Template={id:string;name:string;description:string;category:string;steps:Array<unknown>};
export default function MarketplaceView(){const[templates,setTemplates]=useState<Template[]>([]);const[busy,setBusy]=useState('');useEffect(()=>{fetch('/api/marketplace',{cache:'no-store'}).then(r=>r.json()).then(p=>setTemplates(p.templates??[])).catch(()=>undefined)},[]);const install=async(id:string)=>{setBusy(id);await fetch('/api/marketplace',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({templateId:id})});setBusy('');};return <div className="mx-auto max-w-7xl space-y-6 p-6"><div><h1 className="text-3xl font-bold">Automation Marketplace</h1><p className="mt-2 text-sm text-muted-foreground">Templates operacionais prontos para instalar no Oryon.</p></div><div className="grid gap-4 md:grid-cols-2 xl:grid-cols-3">{templates.map(t=><Card key={t.id} className="gradient-surface border-0 rounded-2xl"><CardHeader><CardTitle className="text-lg">{t.name}</CardTitle></CardHeader><CardContent><div className="text-xs uppercase tracking-wide text-muted-foreground">{t.category}</div><p className="mt-2 text-sm text-muted-foreground">{t.description}</p><div className="mt-3 text-xs">{t.steps.length} passos</div><Button className="mt-4" onClick={()=>void install(t.id)} disabled={busy===t.id}>{busy===t.id?'A instalar...':'Instalar template'}</Button></CardContent></Card>)}</div></div>}
