'use client';
import '@xyflow/react/dist/style.css';
import { addEdge, Background, Controls, MiniMap, ReactFlow, useEdgesState, useNodesState, type Connection, type Edge, type Node } from '@xyflow/react';
import { useMemo, useState } from 'react';
import { useMutation, useQuery, useQueryClient } from '@tanstack/react-query';
import { Activity, Bot, CheckCircle2, GitBranch, Mail, Play, Plus, Save, Sparkles, Timer, Webhook, X } from 'lucide-react';
import { OryonBadge, OryonButton, OryonCard, OryonEmptyState, OryonInput, OryonPanel, OryonSelect } from '@/components/oryon-ui';
import { useToast } from '@/hooks/use-toast';
const PALETTE=[['trigger.form.submitted','Trigger · Form submitted',Webhook],['trigger.record.created','Trigger · Record created',Plus],['trigger.status.changed','Trigger · Status changed',Activity],['action.create.task','Action · Create task',CheckCircle2],['action.request.approval','Action · Request approval',CheckCircle2],['action.notify','Action · Send notification',Mail],['action.condition','Logic · Condition',GitBranch],['action.wait','Wait',Timer],['action.ai','Action · Invoke AI',Bot]] as const;
type VisualWorkflow={id:string;name:string;description?:string;version?:number;workflowId?:string;automationId?:string;graph?:{nodes?:Array<Record<string,unknown>>;edges?:Array<Record<string,unknown>>}};type FlowData={label:string;type:string;config:Record<string,unknown>};
async function fetchVisualWorkflows():Promise<VisualWorkflow[]>{const r=await fetch('/api/workflow-platform?resource=visual-workflows',{cache:'no-store',credentials:'include'});const p=await r.json();if(!r.ok)throw new Error(p.error??'Falha ao carregar workflows.');return Array.isArray(p.data)?p.data:[]}
function normalizeNodes(graph:VisualWorkflow['graph']):Node<FlowData>[] {return(graph?.nodes??[]).map((item,index)=>({id:String(item.id??`node_${index+1}`),position:{x:Number(item.x??80+index*240),y:Number(item.y??100)},data:{label:String(item.label??item.type??'Node'),type:String(item.type??'action'),config:(item.config&&typeof item.config==='object'?item.config:{}) as Record<string,unknown>}}))}function normalizeEdges(graph:VisualWorkflow['graph']):Edge[]{return(graph?.edges??[]).map((item,index)=>({id:String(item.id??`edge_${index+1}`),source:String(item.source??''),target:String(item.target??''),label:typeof item.label==='string'?item.label:undefined})).filter(e=>e.source&&e.target)}
export default function ReactFlowWorkflowBuilder(){const{toast}=useToast();const qc=useQueryClient();const workflows=useQuery({queryKey:['visual-workflows'],queryFn:fetchVisualWorkflows,staleTime:15000});const[selectedWorkflow,setSelectedWorkflow]=useState<VisualWorkflow|null>(null);const[name,setName]=useState('Novo workflow visual');const[description,setDescription]=useState('');const[nodes,setNodes,onNodesChange]=useNodesState<FlowData>([]);const[edges,setEdges,onEdgesChange]=useEdgesState([]);const[selectedNodeId,setSelectedNodeId]=useState<string|null>(null);const[counter,setCounter]=useState(0);const selectedNode=nodes.find(n=>n.id===selectedNodeId)??null;const graph=useMemo(()=>({nodes:nodes.map(n=>({id:n.id,type:n.data.type,label:n.data.label,x:n.position.x,y:n.position.y,config:n.data.config})),edges:edges.map(e=>({id:e.id,source:e.source,target:e.target,label:e.label}))}),[nodes,edges]);const save=useMutation({mutationFn:async()=>{const r=await fetch('/api/workflow-platform',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({action:'visual.save',id:selectedWorkflow?.id,input:{name,description,active:true,graph}})});const p=await r.json();if(!r.ok)throw new Error(p.error??'Falha ao guardar workflow.');return p.data as VisualWorkflow},onSuccess:async(w)=>{setSelectedWorkflow(w);toast({title:'Workflow guardado',description:`Versão ${w?.version??'n/a'}`});await qc.invalidateQueries({queryKey:['visual-workflows']})},onError:e=>toast({variant:'destructive',title:'Erro',description:e.message})});const publish=useMutation({mutationFn:async(id:string)=>{const r=await fetch('/api/workflow-platform',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({action:'visual.publish',id})});const p=await r.json();if(!r.ok)throw new Error(p.error??'Falha ao publicar.');return p.data},onSuccess:p=>toast({title:'Publicado',description:`Workflow ${p?.workflowId??'n/a'} · automation ${p?.automationId??'n/a'}`}),onError:e=>toast({variant:'destructive',title:'Publicação falhou',description:e.message})});const run=useMutation({mutationFn:async(id:string)=>{const r=await fetch('/api/workflow-platform',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({action:'execution.run',automationId:id,payload:{testRun:true,source:'oryon-react-flow'}})});const p=await r.json();if(!r.ok)throw new Error(p.error??'Falha no test run.');return p.data},onSuccess:p=>toast({title:'Test run colocado na fila',description:`Job ${p?.jobId??'n/a'}`}),onError:e=>toast({variant:'destructive',title:'Test run falhou',description:e.message})});const reset=()=>{setSelectedWorkflow(null);setName('Novo workflow visual');setDescription('');setNodes([]);setEdges([]);setSelectedNodeId(null)};const edit=(w:VisualWorkflow)=>{setSelectedWorkflow(w);setName(w.name);setDescription(w.description??'');const next=normalizeNodes(w.graph);setNodes(next);setEdges(normalizeEdges(w.graph));setSelectedNodeId(next[0]?.id??null)};const addNode=(type:string)=>{const id=`rf_${Date.now()}_${counter+1}`;setCounter(v=>v+1);const item=PALETTE.find(([key])=>key===type);setNodes(current=>[...current,{id,position:{x:80+(current.length%3)*240,y:80+Math.floor(current.length/3)*140},data:{label:String(item?.[1]??type),type,config:{}}}]);setSelectedNodeId(id)};const patchNode=(patch:Partial<FlowData>)=>{if(!selectedNodeId)return;setNodes(current=>current.map(n=>n.id===selectedNodeId?{...n,data:{...n.data,...patch}}:n))};const patchConfig=(key:string,value:string)=>{if(!selectedNodeId)return;setNodes(current=>current.map(n=>n.id===selectedNodeId?{...n,data:{...n.data,config:{...n.data.config,[key]:value}}}:n))};const onConnect=(c:Connection)=>setEdges(current=>addEdge({...c,animated:false},current));return <div className="grid gap-4"><div className="flex flex-col gap-4 lg:flex-row lg:items-end lg:justify-between"><div><p className="text-label text-primary">Automate</p><h1 className="mt-1 text-h1">Workflow Builder</h1><p className="mt-1 text-body-small text-muted-foreground">Canvas visual único do Oryon. Trigger, logic, action, condition e wait, com persistência real.</p></div><div className="flex flex-wrap gap-2"><OryonButton variant="outline" size="sm" onClick={reset}><Plus className="h-3.5 w-3.5"/>Novo</OryonButton><OryonButton size="sm" onClick={()=>save.mutate()} disabled={save.isPending}><Save className="h-3.5 w-3.5"/>{save.isPending?'A guardar…':'Guardar'}</OryonButton>{selectedWorkflow?.id?<OryonButton variant="secondary" size="sm" onClick={()=>publish.mutate(selectedWorkflow.id)} disabled={publish.isPending}>Publicar</OryonButton>:null}{selectedWorkflow?.automationId?<OryonButton variant="outline" size="sm" onClick={()=>run.mutate(selectedWorkflow.automationId!)} disabled={run.isPending}><Play className="h-3.5 w-3.5"/>Test run</OryonButton>:null}</div></div><div className="grid gap-4 xl:grid-cols-[220px_minmax(0,1fr)_300px]"><OryonPanel className="h-[640px] overflow-hidden p-3"><p className="px-2 py-2 text-label">Node library</p><div className="grid gap-1.5 overflow-y-auto pr-1 custom-scrollbar">{PALETTE.map(([type,label,Icon])=><button key={type} type="button" onClick={()=>addNode(type)} className="flex items-center gap-2 rounded-[8px] border border-transparent bg-surface-1 px-2.5 py-2 text-left text-[11px] text-muted-foreground hover:border-border hover:bg-surface-2 hover:text-foreground"><Icon className="h-3.5 w-3.5 text-primary"/><span className="min-w-0 flex-1 truncate">{label}</span></button>)}</div></OryonPanel><OryonPanel className="h-[640px] overflow-hidden p-0"><div className="flex h-11 items-center justify-between border-b border-border px-3"><div><p className="text-label">Canvas</p><p className="text-[10px] text-muted-foreground">{nodes.length} nodes · {edges.length} connections</p></div><div className="flex gap-1.5"><OryonBadge>React Flow</OryonBadge><OryonBadge tone="accent">Live graph</OryonBadge></div></div><div className="h-[596px]"><ReactFlow nodes={nodes} edges={edges} onNodesChange={onNodesChange} onEdgesChange={onEdgesChange} onConnect={onConnect} onNodeClick={(_,node)=>setSelectedNodeId(node.id)} fitView><Background gap={24} size={1}/><MiniMap/><Controls/></ReactFlow></div></OryonPanel><OryonPanel className="h-[640px] overflow-hidden p-0"><div className="flex h-11 items-center justify-between border-b border-border px-4"><p className="text-label">Inspector</p>{selectedNode?<OryonButton variant="ghost" size="icon" onClick={()=>setSelectedNodeId(null)} aria-label="Fechar inspector"><X className="h-3.5 w-3.5"/></OryonButton>:null}</div><div className="h-[596px] overflow-y-auto p-4 custom-scrollbar">{selectedNode?<div className="grid gap-4"><div><p className="text-[12px] font-semibold text-foreground">{selectedNode.data.label}</p><p className="mt-1 text-[10px] text-muted-foreground">{selectedNode.data.type}</p></div><OryonInput label="Label" value={selectedNode.data.label} onChange={e=>patchNode({label:e.target.value})}/><OryonSelect label="Node type" value={selectedNode.data.type} onChange={e=>patchNode({type:e.target.value})}>{PALETTE.map(([type,label])=><option key={type} value={type}>{label}</option>)}</OryonSelect><OryonInput label="Reference" value={String(selectedNode.data.config.reference??'')} onChange={e=>patchConfig('reference',e.target.value)} placeholder="payload.reference"/><OryonInput label="Value / target" value={String(selectedNode.data.config.value??'')} onChange={e=>patchConfig('value',e.target.value)} placeholder="Ex.: task.completed"/><OryonButton variant="danger" onClick={()=>{setNodes(current=>current.filter(n=>n.id!==selectedNodeId));setSelectedNodeId(null)}}>Remover node</OryonButton></div>:<OryonEmptyState icon={<GitBranch className="h-5 w-5"/>} title="Select a node" description="Escolha um elemento no canvas para configurar os seus parâmetros."/>}</div></OryonPanel></div><OryonPanel className="p-4"><div className="grid gap-2 lg:grid-cols-2"><OryonInput label="Nome do workflow" value={name} onChange={e=>setName(e.target.value)} placeholder="Nome"/><label className="grid gap-1.5"><span className="text-[10px] font-semibold uppercase tracking-[0.08em] text-muted-foreground">Descrição</span><textarea className="min-h-9 w-full rounded-[8px] border border-border bg-surface-1 px-3 py-2 text-[13px] text-foreground outline-none placeholder:text-muted-foreground/65 focus:border-primary/70" value={description} onChange={e=>setDescription(e.target.value)} placeholder="Descrição do workflow"/></label></div></OryonPanel></div>}
